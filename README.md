Sources for the plugin `plugin.appodeal.MillenialMedia`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.MillenialMedia" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
